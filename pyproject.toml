[project]
name = "jobs-rag"
version = "0.1.0"
description = ""
authors = [
    {name = "regisrex",email = "ndizihiweregis06@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "feedparser (>=6.0.12,<7.0.0)",
    "ollama (>=0.4.0,<1.0.0)",
    "gel (>=3.1.0,<4.0.0)",
]

[tool.poetry]
packages = [{include = "ingestion"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
